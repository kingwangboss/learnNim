{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nimbuild",
            "type": "shell",
            "command": "nim",
            "args": ["compile", "--debugger:native", "${file}"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}